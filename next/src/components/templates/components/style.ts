import styled from 'styled-components';

const Wrapper = styled.div`
  max-width: 1020px;
  margin: 0 auto;
  padding: 20px;
`;

const Title = styled.h1`
  font-size: 30px;
  margin-bottom: 30px;
`;

const ComponentTypeTitle = styled.h2`
  font-size: 26px;
  margin-bottom: 20px;
`;

const ComponentTypeWrap = styled.div`
  margin-bottom: 50px;
`;

const ComponentWrap = styled.div`
  margin: 20px 0;
`;

const ComponentTitle = styled.h3`
  font-size: 20px;
  margin-bottom: 5px;
`;

const exportDefault = {
  Wrapper,
  Title,
  ComponentTypeTitle,
  ComponentTypeWrap,
  ComponentWrap,
  ComponentTitle,
};

export default exportDefault;
