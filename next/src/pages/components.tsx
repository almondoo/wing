import type { NextPage } from 'next';
import Template from '../components/templates/components/index';

const Components: NextPage = () => {
  return <Template />;
};

export default Components;
